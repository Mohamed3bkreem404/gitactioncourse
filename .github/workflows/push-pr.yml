name: hoho

on:
  push:
      branches:
        - main
        # this mean trigger will work on any  branche that starts with release
        - "release/**"
      paths:
        - "src/**"
  pull_request:
      branches:
      - maina
      - "release/**"
      paths:
        - "src/**"

jobs:
  welcome-message:
    runs-on: ubuntu-latest

    steps:
      - name: say welcome job
        run: echo "hey welcome job :3"

      - name: checkout code
        uses: actions/checkout@v4.2.2
       